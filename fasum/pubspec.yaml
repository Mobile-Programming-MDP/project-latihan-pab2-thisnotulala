name: fasum
description: "A new Flutter project."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.4.4 <4.0.0"
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  firebase_core: ^3.13.0
  firebase_auth: ^5.5.2
  firebase_data_connect: ^0.1.4
  cloud_firestore: ^5.6.6
  intl: ^0.19.0
  geolocator: ^13.0.2
  image_compress: ^0.0.2
  flutter_image_compress: ^2.4.0
  image_picker: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
flutter:
  uses-material-design: true
